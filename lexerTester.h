
/*
    ifstream ifs("test.txt");
    Token command;

    while((command = getTok(ifs)) != Token::tok_eof)
    {
        switch(command)
        {
            case Token::tok_def:
                cout << "def" << endl;
                break;
            case Token::tok_eof:
                cout << "eof" << endl;
                break;
            case Token::tok_extern:
                cout << "extern" << endl;
                break;
            case Token::tok_identifier:
                cout << "identifier: " << IdentifierStr << endl;
                break;
            case Token::tok_number:
                cout << "number: " << NumVal << endl;
                break;
            case Token::tok_operator:
                cout << "operator: " << Operator << endl;            
        }
    }
*/